# Folder Note

This note has no explicit frontmatter but should be public because it's in a folder with.

The folder's .pluie file sets: true all in this folder.

TEST EDIT - checking if file watcher detects this change.
